package common; 

public enum Action {
    // Reservation Actions
    GET_RESERVATION,
    UPDATE_RESERVATION,
    CANCEL_RESERVATION,
    RESERVATION_NOT_FOUND,
    SEND_VERIFICATION_CODE,
    
    // Guest Actions
    GET_GUEST,
    UPDATE_GUEST,
    
    // Connection Actions
    DISCONNECT
    
    
}
